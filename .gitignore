# Secrets & local env
.env
.env.*
!.env.example

# Virtualenvs / caches
.venv/
__pycache__/
*.pyc
.DS_Store
.uv_cache/
.ruff_cache/

# Run outputs (large / raw / regenerable) — the aggregated CSVs in results/ ARE tracked
logs/
experiments/results/run_costs/

# CoLLAB is cloned separately (see README), never committed
CoLLAB_SEA/
external/
